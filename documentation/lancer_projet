côté angular : 

0) lancer le fichier Application.java depuis intellij
1) il faut se placer dans src.main.java.nate.company.java.angular.angularclient
2) tapez : ng serve --open (lancer angular)

ng serve --open

côté back : 


- appuyez simplement sur le bouton play de intellij si le back end a bien été build avant (il n'est nécessaire de ne build qu'une seule fois si tout s'est bien passé lors du premier build).

````shell
java -cp h2-2.3.232.jar org.h2.tools.Server -ifNotExists &
```


avec docker :

```bash

cd "./Historique_oeuvres"

```

(optionnel)
si le container n'existe pas :

```

docker container create -i -t --name museeoeuvrecontainer openjdk:23-jdk

```

(fin optionnel)

ensuite lancer le container :

```bash

 sudo docker build -t museeoeuvrecontainer .

```