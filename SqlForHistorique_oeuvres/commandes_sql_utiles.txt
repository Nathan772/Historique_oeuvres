Lancer mysql avec une session USER :

mysql -u nathanb -p

mdp: 

(voir portable ou dans le dossier dédié)


DATA BASE :

-SHOW DATABASES;

-USE databasaName;

-USE historyDB;

"
source bddinit.sql
"

lance le programme du dossier "bddinit.sql" lorsque c'est lancé
depuis l'invite de commande de mysql.


mysqldump -u nathanb historyDB > db_backup.sql


/*Renames the table user*/
ALTER TABLE user_table RENAME user_table;

/*Renames the column year of the movie table*/
ALTER TABLE movie RENAME COLUMN year TO yearOfRelease;


